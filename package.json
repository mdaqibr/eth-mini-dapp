{
  "name": "mini-dapp",
  "version": "1.0.0",
  "scripts": {
    "node": "npx hardhat node",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "listen": "npx hardhat run scripts/listenEvents.js --network localhost"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^6.1.0"
  },
  "dependencies": {
    "ethers": "^6.16.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.3"
  }
}
